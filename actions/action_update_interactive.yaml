---
name: "action_update_interactive"
pack: freeipa
runner_type: "python-script"
description: "This action will update the interative message (buttons) in mattermost"
enabled: true
entry_point: "action_update_interactive.py"

parameters:
  webhook_data:
    type: "string"
    description: "The webhook data"
    required: true
    position: 0