---
class_name: "SensorPassword"
entry_point: "sensor_password.py"
description: "A sensor that will detect the expirated password"
trigger_types:
  - name: "password_expired_event"
    description: "The password has been expired"
    payload_schema:
      type: "object"
